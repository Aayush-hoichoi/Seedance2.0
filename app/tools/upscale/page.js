import UpscaleClient from './UpscaleClient.jsx';

export const metadata = {
    title: 'Upscale Video — loglineAI Studio',
    description: 'AI video upscaling up to 8K with BytePlus MediaKit.',
};

export default function UpscalePage() {
    return <UpscaleClient />;
}
