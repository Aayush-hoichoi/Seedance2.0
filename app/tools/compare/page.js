import CompareClient from './CompareClient.jsx';

export const metadata = {
    title: 'Compare Video — loglineAI Studio',
    description: 'Play 2 or 4 takes side by side, in sync.',
};

export default function ComparePage() {
    return <CompareClient />;
}
