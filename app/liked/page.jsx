import LikedClient from './LikedClient.jsx';

export const metadata = {
    title: 'Liked — Seedance 2.0 Studio',
    description: 'Every generation marked with a like, across all creators.',
};

export default function LikedPage() {
    return <LikedClient />;
}
